{
  "name": "pdf-highlighter",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd src && npm install && npm run build",
    "start": "gunicorn wsgi:app",
    "dev": "cd src && npm start"
  }
} 